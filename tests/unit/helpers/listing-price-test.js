import { listingPrice } from 'craigslist/helpers/listing-price';
import { module, test } from 'qunit';

module('Unit | Helper | listing price');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingPrice([42]);
  assert.ok(result);
});
